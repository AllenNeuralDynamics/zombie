/** Shared Dynamic Routing context colours. */

// Saturated colours are used for labels, indicators, and traces. The lighter
// pair is reserved for the context bands behind the raster rows.
export const DR_CONTEXT_COLORS = Object.freeze({
  vis: '#16a34a',
  aud: '#f59e0b',
});

export const DR_CONTEXT_BACKGROUND_COLORS = Object.freeze({
  vis: '#dceedd',
  aud: '#fff0d5',
});
